import React from 'react';
import { Box } from '@mui/material';

const Logo = () => {
  return (
    <Box
      sx={{
        width: 40,
        height: 40,
        backgroundColor: 'black', // Placeholder for logo, replace with your logo image
        borderRadius: '0%' // Change to box shape
      }}
    />
  );
};

export default Logo;
